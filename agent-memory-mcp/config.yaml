name: Agent Memory MCP
description: Test minimal Agent Memory MCP pour Home Assistant
version: "0.1.1"
slug: agent_memory_mcp
init: false
arch:
  - amd64
startup: application
boot: auto
host_network: true
ports:
  18080/tcp: 18080
ports_description:
  18080/tcp: MCP Streamable HTTP
map:
  - type: addon_config
    read_only: false
options: {}
schema: {}
